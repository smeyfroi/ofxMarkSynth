//
//  GuiConstants.h
//  ofxMarkSynth
//
//  Named constants for GUI-related values.
//

#pragma once

namespace ofxMarkSynth {

// Thumbnail sizes (pixels)
constexpr float LAYER_THUMB_WIDTH = 128.0f;
constexpr float MEMORY_THUMB_WIDTH = 64.0f;
constexpr float MEMORY_SLOT_HEIGHT = 100.0f;

// Slider dimensions
constexpr float DEFAULT_SLIDER_WIDTH = 200.0f;
constexpr float VERTICAL_SLIDER_HEIGHT = 140.0f;
constexpr float VERTICAL_SLIDER_WIDTH = 24.0f;

// Animation timing
constexpr float HIGHLIGHT_DURATION_SECS = 1.5f;

// Navigation button sizing
constexpr float NAV_BUTTON_SIZE = 60.0f;
constexpr float NAV_BUTTON_SPACING = 20.0f;

// Progress bar heights
constexpr float HIBERNATION_BAR_HEIGHT = 4.0f;

// Config list dimensions
constexpr float CONFIG_LIST_HEIGHT = 120.0f;

} // namespace ofxMarkSynth
